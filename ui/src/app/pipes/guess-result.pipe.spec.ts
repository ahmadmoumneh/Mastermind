import { GuessResultPipe } from './guess-result.pipe';

describe('GuessResultPipe', () => {
  it('create an instance', () => {
    const pipe = new GuessResultPipe();
    expect(pipe).toBeTruthy();
  });
});
